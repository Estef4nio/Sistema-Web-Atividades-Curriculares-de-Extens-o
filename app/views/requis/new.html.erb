<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Nova Requisição</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f8f9fa;
    }

    header {
      background-color: #ffffff;
      color: #ffffff;
      padding: 1rem;
      border-bottom-left-radius: 10px;
      border-bottom-right-radius: 10px;
      text-align: center;
      margin-bottom: 1rem;
    }

    section {
      max-width: 800px;
      margin: 0 auto;
      padding: 2rem;
      background-color: #ffffff;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      border-radius: 10px;
    }

    h1 {
      color: #343a40;
      margin-bottom: 1rem;
    }

    .btn {
      border-radius: 8px;
    }

    .btn-primary {
      background-color: #007bff;
      border-color: #007bff;
    }

    .btn-outline-dark {
      color: #343a40;
      border-color: #343a40;
    }

    .btn-outline-dark:hover {
      background-color: #343a40;
      color: #ffffff;
    }

    .back-link {
      margin-top: 1rem;
    }
  </style>
</head>
<body>

  <header>
    <h1>Nova Requisição</h1>
  </header>

  <section>
    <%= render "form", requi: @requi %>

    <div class="back-link">
      <% if admin_signed_in? %>
        <%= link_to "Voltar para Requisições", requis_path, class: "btn btn-outline-dark" %>
      <% elsif user_signed_in? %>
        <%= link_to "Voltar para Perfil do Usuário", user_profile_path, class: "btn btn-outline-dark" %>
      <% end %>
    </div>
  </section>

</body>
</html>
